# Native FastAPI service. No frontend build or database is required.
services:
  - type: web
    name: saiganesh-portfolio
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    autoDeploy: false
